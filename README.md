# website-checker

<p>The script checks predefined values from a CSV file on URL basis for changes and sends an email if the values differ.</p>

<p>Checks can be e.g.: html-title, meta-description, h1, robots-meta-tag, canonical, statuscode.</p>
